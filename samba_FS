#!/bin/bash

if [ "$EUID" == 0 ]; then

    printf "Choose your configuration\n1 - Configure as a Standalone File Server\n"\
"2 - Configure as Domain Member\n"

    ConfigReset=1
    while [ "${ConfigReset}" == 1 ]; do
	if [ -z "${ConfigMessage}" ]; then
	    ChoiceMessage=1
	elif [ "${ConfigMessage}" ]; then
	    echo "Must enter 1 or 2"
	fi

	read -p ">" ConfigChoice

	if [ "${ConfigChoice}" == 1 ] || [ "${ConfigChoice}" == 2 ]; then
	    ConfigReset=0
	fi
    done


    if [ "${ConfigChoice}" == 1 ]; then
	apt install samba

#	printf "To share a home folder of a user you must create th user in samba using:"\
#"smbpasswd -a username"

#potentially choices for configuration (read only, browsable....)
    elif [ "${ConfigChoice}" == 2 ]; then

	while [ -z "${KerberosRealm}" ]; then
	    if [ -z "${KRBMessage}" ]; then
		KRBMessage=1
	    elif [ "${KRBMessage}" ]; then
		echo "Local IP must be entered"
	    fi
	    read -p "Enter the local IP of this machine:" KerberosRealm
	done

	while [ -z "${LocalIP}" ]; then
	    if [ -z "${LocalIPMessage}" ]; then
		LocalIPMessage=1
	    elif [ "${LocalIPMessage}" ]; then
		echo "Local IP must be entered"
	    fi
	    read -p "Enter the local IP of this machine:" LocalIP
	done

	apt install acl attr samba samba-dsdb-modules samba-vfs-modules winbind libpam-winbind libnss-winbind libpam-krb5 krb5-config krb5-user dnsutils

	printf "\nnameserver ${LocalIP}\nsearch ${KerberosRealm}\n" >> /etc/resolvconf/resolv.conf.d/head
	systemctl restart resolvconf

	printf "[libdefaults]\n default_realm = ${KerberosRealm}\ndns_lookup_realm = false\ndns_lookup_kdc = true" > /etc/krb5.conf


else

    printf "This script must be run as root"

fi
